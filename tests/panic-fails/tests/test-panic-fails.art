import.version:3.0.0 {unitt}! 
import {src/panic-fails}!

describe "Panic fails a test suite" [
    it "This test should pass" [
        expects.be:'equal? @[3 shouldNotPanic]
    ]

    it "This test should fail" [
        expects shouldPanic
    ]
]
